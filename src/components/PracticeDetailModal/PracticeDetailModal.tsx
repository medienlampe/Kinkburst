import React from "react";

/**
 * Overlay opened on right-click of a practice field to view and edit its context note.
 * Practices with a note get an asterisk ("*") appended to their title in the scale.
 *
 * TODO: implement per docs/Outline.md ("General Idea").
 */
const PracticeDetailModal = (): JSX.Element => {
  return <></>;
};

export default PracticeDetailModal;
