import React, { type JSX } from "react";

/**
 * Controls the people the board is for (default: one person). The names are shown in the
 * board title, e.g. "Smorkinkboard (for Person A, Person B and Person C)".
 *
 * TODO: implement per docs/Outline.md ("General Idea").
 */
const PersonsBar = (): JSX.Element => {
  return <></>;
};

export default PersonsBar;
